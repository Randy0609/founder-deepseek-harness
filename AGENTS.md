# AGENTS.md

本仓库是「创业者-DeepSeek Harness」的公开插件源码入口。当前为公开起步版，尚无可安装插件。开始任务先读 `README.md`、`docs/ARCHITECTURE.md` 和直接相关的插件文件。

## 工作方式

- 以锁定的 DeepSeek Harness 版本及其公开扩展接口为实现依据；官方 `master` 文档只作研究线索。
- 每个插件应有单一清楚的职责。复用官方会话、输入框、项目和右侧文件能力；确认缺口后再实现扩展。
- 新包须说明入口、依赖、配置、版本范围、权限、失败路径、停用方式和可重复的验收步骤。
- 状态只用 `planned`、`prototype`、`verified` 等有证据支持的词。文档或截图不等于运行验收。
- 推荐社区插件时逐项核对来源、许可证、DSH 装载声明、锁定版本、数据权限与隔离运行结果；GitHub Topic 和作者 README 只能提供候选线索。
- 从干净 Profile 验证，不以维护者的个人环境、会话和凭据为默认前提。

## 公开内容约束

- 只提交原创内容、已核许可的第三方材料和合成示例。
- 不提交 API key、Token、Cookie、私钥、`.env`、本机 Profile、会话记录、客户或财务数据、内部地址和未公开业务策略。
- 不直接复制私人或业务实例的代码、样式包、截图、目录树与配置；抽象出通用需求后重新实现。
- 不把第三方商标或图标当成本项目自己的品牌资产。
- 发布前运行 `docs/RELEASE-CHECKLIST.md`，复核实际待发布文件与来源。

## 变更与完成标准

- 先确定目标插件和影响范围，再改最少文件；不创建空包或假入口。
- 只有构建、隔离启动、关键行为和撤销路径都实测，才把插件标为 `verified`。
- 本地修改、提交、创建 GitHub 仓库、推送和发布分别记录，不以其中一步代替另一步。
- 公开仓库创建和首次发布由项目所有者在审阅后执行或明确授权。
